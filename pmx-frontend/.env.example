# API base URL used by the frontend (client-side).
# In dev, leave as `/api` and let Vite proxy forward to the backend.
VITE_API_URL=/api

# Where the Vite dev proxy forwards `/api/*` requests.
# Should match your FastAPI uvicorn host:port.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
