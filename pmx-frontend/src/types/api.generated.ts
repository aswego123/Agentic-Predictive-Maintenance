/**
 * Auto-generated OpenAPI types — placeholder.
 * Regenerate with `npm run gen:api-types` while the FastAPI backend
 * is running. Everything else in the app should keep importing from
 * `@/types/domain` so that this file can be swapped in later without
 * touching call sites.
 */
export {};
